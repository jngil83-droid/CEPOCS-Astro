---
import Layout from '../layouts/Layout.astro';

const fotos = [
  "https://i.postimg.cc/zDHRSgjS/IMG-20230418-151548891.jpg",
  "https://i.postimg.cc/yYPmwNKv/Captura-de-pantalla-2026-05-22-235256.png",
  "https://i.postimg.cc/Xvsm8HHg/IMG-20260520-WA0001.jpg",
  "https://i.postimg.cc/xj6FHB6j/IMG-20230419-103850818.webp",
  "https://i.postimg.cc/JhX95KGb/IMG-20241128-WA0063.jpg",
  "https://i.postimg.cc/Mp3Qmbhd/IMG-20221027-131155256.webp",
  "https://i.postimg.cc/C5vhpJCb/IMG-20230416-115152619.jpg",
  "https://i.postimg.cc/cHS4Rv46/IMG-20250117-WA0001.jpg",
  "https://i.postimg.cc/GmTc6F4P/IMG-20230418-161547348.webp",
  "https://i.postimg.cc/d0MqFhCW/IMG-20230418-113445457.webp",
  "https://i.postimg.cc/ZKMbf7KM/IMG-20230416-121658195.jpg",
  "https://i.postimg.cc/6q992F6P/Whats-App-Image-2026-05-15-at-17-12-35.jpg",
  "https://i.postimg.cc/cCssgP1D/Whats-App-Image-2026-05-15-at-17-13-48.jpg",
  "https://i.postimg.cc/L6QH5yc3/Whats-App-Image-2026-05-15-at-17-15-40.jpg",
  "https://i.postimg.cc/ZKsTnfGM/Whats-App-Image-2026-05-15-at-17-15-42.jpg",
  "https://i.postimg.cc/HsvpnZGm/Whats-App-Image-2026-05-15-at-17-15-43.jpg",
  "https://i.postimg.cc/mk8TQVtw/Whats-App-Image-2026-05-15-at-17-15-44.jpg",
  "https://i.postimg.cc/jjpxcbtv/IMG-20220527-WA0014.jpg",
  "https://i.postimg.cc/5tY980dF/IMG-20230418-115613812.webp",
  "https://i.postimg.cc/jSkdWf9J/IMG-20230418-120706993.webp",
  "https://i.postimg.cc/jjwqy50z/IMG-20230418-132913262.webp",
  "https://i.postimg.cc/Pqpf1xsS/IMG-20230418-164600436.webp",
  "https://i.postimg.cc/6p157ZSK/IMG-20230418-172722096.webp",
  "https://i.postimg.cc/N0SYD5s4/IMG-20230418-171802665.jpg",
  "https://i.postimg.cc/15bt7SvP/Whats-App-Image-2026-05-18-at-20-32-27.jpg",
  "https://i.postimg.cc/tC8TvbBp/Whats-App-Image-2026-05-18-at-20-32-25.jpg",
  "https://i.postimg.cc/Gh62gRXb/Whats-App-Image-2026-05-18-at-20-32-25s.jpg"
];

const videos = [
  { url: "https://www.youtube.com/embed/4Ph5ZhSizh8", titulo: "Contra los feminicidios" },
  { url: "https://www.youtube.com/embed/kCRBCTF6XY4", titulo: "Prueba Café Paradero" },
  { url: "https://www.youtube.com/embed/UKU0S8u2ruE", titulo: "Más independiente gracias a CEPOCS" },
  { url: "https://www.youtube.com/embed/K8IXvDBhaPI", titulo: "Mujeres de la costa de Montecristi y ganadería caprina" },
  { url: "https://www.youtube.com/embed/lKwXbkYBCCc", titulo: "Cierre del proyecto de fondos Canadá" },
  { url: "https://www.youtube.com/embed/zExraq8ixHY", titulo: "Proyecto CEPOCS - 15 dic 2025" },
  { url: "https://www.youtube.com/embed/rc-8jl9uvOI", titulo: "Proyecto fondo Canadá - Cordillera" },
  { url: "https://www.youtube.com/embed/C9UHnD88mFw", titulo: "Oficina provincial Valverde" },
  { url: "https://www.youtube.com/embed/CZwkFqQhOZU", titulo: "Nueva Oficina en Navarrete" }
];
---

<Layout title="Multimedia | CEPOCS">
<section class="multimedia-hero">
  <span class="hero-tag">
    <i class="fas fa-camera"></i>
    Archivo Multimedia
  </span>

  <h1>Nuestra Historia en Imágenes</h1>
  <p>
    Fotografías, videos y testimonios que reflejan el trabajo realizado por CEPOCS junto a las comunidades rurales de la Cordillera Septentrional.
  </p>
</section>

<main class="multimedia-container">

  <section class="section-block">
    <h2 id="fotos">Fotografías</h2>

  <div id="galleryGrid" class="gallery-grid">
  {
    fotos.map((url, index) => (
      <figure
        class={`gallery-card ${index >= 8 ? 'hidden-photo' : ''}`}
        data-index={index}
      >
        <button class="gallery-thumb" aria-label={`Abrir fotografía ${index + 1}`}>
          <img
            src={url}
            alt={`Fotografía CEPOCS ${index + 1}`}
            loading="lazy"
            decoding="async"
          />
        </button>
        <figcaption class="sr-only">Fotografía CEPOCS {index + 1}</figcaption>
      </figure>
    ))
  }
  </div>

  <button id="togglePhotos" class="load-more-btn" type="button" aria-expanded="false" aria-controls="galleryGrid">
    Ver más fotografías
  </button>

  <!-- Lightbox modal -->
  <div id="lightbox" class="lightbox" role="dialog" aria-modal="true" aria-hidden="true" aria-label="Visor de imágenes">
    <div class="lightbox-inner">
      <button id="lbClose" class="lb-close" aria-label="Cerrar">✕</button>
      <button id="lbPrev" class="lb-nav lb-prev" aria-label="Imagen anterior">◀</button>
      <div class="lb-media">
        <img id="lbImage" src="" alt="" />
        <p id="lbCaption" class="lb-caption"></p>
      </div>
      <button id="lbNext" class="lb-nav lb-next" aria-label="Siguiente imagen">▶</button>
    </div>
  </div>

  </section>

  <section class="section-block">
    <h2 id="videos">Videos</h2>

    <div id="videoGrid" class="video-grid">
{
  videos.map((video, index) => (
    <article
      class={`video-card ${index >= 6 ? 'hidden-video' : ''}`}
    >
      <iframe
        src={video.url}
        title={video.titulo}
        loading="lazy"
        allowfullscreen>
      </iframe>

      <h3>{video.titulo}</h3>
    </article>
  ))
}
</div>

<button id="toggleVideos" class="load-more-btn" type="button" aria-expanded="false" aria-controls="videoGrid">
  Ver más vídeos
</button>
  </section>

</main>
<script is:inline>
// Toggle photos/videos
const photoBtn = document.getElementById('togglePhotos');
const galleryGrid = document.getElementById('galleryGrid');
let photosOpen = false;
photoBtn?.addEventListener('click', () => {
  document.querySelectorAll('.hidden-photo').forEach(el => el.style.display = photosOpen ? 'none' : 'block');
  photosOpen = !photosOpen;
  photoBtn.textContent = photosOpen ? 'Ver menos fotografías' : 'Ver más fotografías';
  photoBtn.setAttribute('aria-expanded', String(photosOpen));
});

const videoBtn = document.getElementById('toggleVideos');
let videosOpen = false;
videoBtn?.addEventListener('click', () => {
  document.querySelectorAll('.hidden-video').forEach(el => el.style.display = videosOpen ? 'none' : 'block');
  videosOpen = !videosOpen;
  videoBtn.textContent = videosOpen ? 'Ver menos vídeos' : 'Ver más vídeos';
});

// Lightbox
const thumbs = Array.from(document.querySelectorAll('.gallery-thumb'));
const lightbox = document.getElementById('lightbox');
const lbImage = document.getElementById('lbImage');
const lbCaption = document.getElementById('lbCaption');
const lbClose = document.getElementById('lbClose');
const lbPrev = document.getElementById('lbPrev');
const lbNext = document.getElementById('lbNext');
let currentIndex = 0;

function openLightbox(index){
  const url = thumbs[index].querySelector('img').src;
  const alt = thumbs[index].querySelector('img').alt || '';
  lbImage.src = url;
  lbImage.alt = alt;
  lbCaption.textContent = alt || `Fotografía ${index+1}`;
  lightbox.setAttribute('aria-hidden', 'false');
  lightbox.classList.add('open');
  currentIndex = index;
  lbClose.focus();
}

function closeLightbox(){
  lightbox.setAttribute('aria-hidden', 'true');
  lightbox.classList.remove('open');
  lbImage.src = '';
}

function showNext(){
  currentIndex = (currentIndex + 1) % thumbs.length;
  openLightbox(currentIndex);
}

function showPrev(){
  currentIndex = (currentIndex - 1 + thumbs.length) % thumbs.length;
  openLightbox(currentIndex);
}

thumbs.forEach((btn, i) => btn.addEventListener('click', (e)=>{ e.preventDefault(); openLightbox(i); }));
lbClose?.addEventListener('click', closeLightbox);
lbNext?.addEventListener('click', (e)=>{ e.stopPropagation(); showNext(); });
lbPrev?.addEventListener('click', (e)=>{ e.stopPropagation(); showPrev(); });

lightbox?.addEventListener('click', (e)=>{ if(e.target === lightbox) closeLightbox(); });
document.addEventListener('keydown', (e)=>{
  if(!lightbox.classList.contains('open')) return;
  if(e.key === 'Escape') closeLightbox();
  if(e.key === 'ArrowRight') showNext();
  if(e.key === 'ArrowLeft') showPrev();
});

</script>
</Layout>

<style>
.multimedia-container{
  max-width:1200px;
  margin:auto;
  padding:20px 20px 10px;
}

.multimedia-hero{
  padding:20px 20px 30px;
  text-align:center;
  background:linear-gradient(135deg, var(--verde), #0d3a2b);
}

.hero-tag{
  display:inline-flex;
  align-items:center;
  gap:.5rem;
  padding:.6rem 1rem;
  border-radius:999px;
  background:#eef5ed;
  color:var(--verde);
  font-weight:600;
  margin-bottom:1.5rem;
}

.multimedia-hero h1{
  font-size:clamp(2rem,5vw,3.5rem);
  color:var(--verde);
  margin-bottom:1rem;
}

.multimedia-hero p{
  max-width:760px;
  margin:auto;
  color:var(--gris);
  line-height:1.8;
}

.section-block{
  margin-top:70px;
}

.section-block h2{
  color:var(--naranja);
  margin-bottom:30px;
  padding-bottom:10px;
  border-bottom:2px solid #eee;
  font-size:1.9rem;
}

.gallery-grid{
  display:grid;
  grid-template-columns:
  repeat(auto-fill,minmax(260px,1fr));
  gap:22px;
}

.gallery-card{
  overflow:hidden;
  border-radius:16px;
  box-shadow:0 8px 24px rgba(0,0,0,.08);
  background:#fff;
}

.gallery-card img{
  width:100%;
  height:260px;
  object-fit:cover;
  display:block;
  transition:.45s;
}

.gallery-card:hover img{
  transform:scale(1.08);
}

/* Lightbox styles */
.gallery-thumb{border:0;background:transparent;padding:0;display:block;width:100%;height:100%;text-align:left}
.lightbox{position:fixed;inset:0;display:none;align-items:center;justify-content:center;background:rgba(0,0,0,.75);z-index:3000}
.lightbox.open{display:flex}
.lightbox-inner{position:relative;max-width:1100px;width:95%;max-height:90%;display:flex;align-items:center;gap:12px}
.lb-media{flex:1;display:flex;flex-direction:column;align-items:center}
.lb-media img{max-width:100%;max-height:80vh;border-radius:8px;box-shadow:0 12px 40px rgba(0,0,0,.5)}
.lb-caption{color:#fff;margin-top:12px;text-align:center}
.lb-close,.lb-nav{position:absolute;background:rgba(255,255,255,.06);border:0;color:#fff;padding:.6rem .8rem;border-radius:6px;cursor:pointer}
.lb-close{right:6px;top:6px}
.lb-prev{left:6px}
.lb-next{right:6px}


.video-grid{
  display:grid;
  grid-template-columns:
  repeat(auto-fit,minmax(340px,1fr));
  gap:30px;
}

.video-card{
  background:#fff;
  border-radius:16px;
  overflow:hidden;
  box-shadow:0 8px 24px rgba(0,0,0,.08);
}

.video-card iframe{
  width:100%;
  height:230px;
  border:none;
}

.video-card h3{
  padding:18px;
  margin:0;
  color:var(--texto);
  font-size:1rem;
}

@media(max-width:768px){

  .multimedia-hero{
    padding:120px 20px 60px;
  }

  .gallery-grid{
    grid-template-columns:1fr 1fr;
    gap:14px;
  }

  .gallery-card img{
    height:180px;
  }

  .video-grid{
    grid-template-columns:1fr;
  }

}

@media(max-width:480px){

  .gallery-grid{
    grid-template-columns:1fr;
  }

}
</style>